export NPM_CONFIG_USERCONFIG=$PWD/.npmrc

npm_registry_url="$(npm config get registry)"
echo "Current npm registry URL: $npm_registry_url"

if [[ "$npm_registry_url" != *"codeartifact"* ]]; then
  source ./npm-auth.sh
fi
